{% block grid %}{% spaceless %}
    {
    "{{ table.type }}" : {
    "attrib" : [
    {%- for flavor, attribs in table.attr %} { "{{ flavor }}" : "{# {{ attribs }} #}"}{% if not loop.last %} , {% endif %}{%- endfor -%}],
        "sections" :
    [ {%- for section in table.section %}
        {% include 'LighthartGridBundle:Section:section.json.twig' with {'section' : section } %} {% if not loop.last %} , {% endif -%}
    {%- endfor -%} ]
    }
    }
{% endspaceless %}{% endblock grid %}
