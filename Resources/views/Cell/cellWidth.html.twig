{%- if boolean -%}{% set widthVal = "4em" %}
{%- else -%}
    {%- if width is defined -%}
        {% set widthVal = width %}
    {%- elseif cell.actions is defined -%}
        {%- if cell.actions|length > buttonNum -%}
            {% set widthVal = buttonWidth * buttonNum + buttonPadding %}
        {%- else -%}
            {% set widthVal = buttonWidth * cell.actions|length + buttonPadding %}
        {%- endif -%}
        {% set widthVal = widthVal~"px" %}
    {%- endif -%}
{%- endif -%}
{% if widthVal is defined %}
style ="width:{{widthVal}};min-width:{{widthVal}};white-space:nowrap;"
{% else %}
style ="white-space:nowrap;"
{% endif %}
{# This should probably get fixed to handle removing widht from attrs at php-level #}