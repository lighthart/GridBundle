{% block newButton -%}
    {%- if newPath is defined -%}
        <a href="{{newPath}}" class="btn btn-xs btn-default" id="lg-new-button">
            {%- if newIcon is defined -%}
                <span class="fa {{newIcon}}"></span>
            {%- endif %} New
        </a>
    {%- endif -%}
{%- endblock newButton %}